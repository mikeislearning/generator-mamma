describe("put a description in here", function() {

  beforeEach(function() {
    //some can go here that happens before every test
  });

  it("allows addition to work properly", function() {
    expect(1+1).toEqual(2);
  });
});


//REMEMBER - every test name must end in Spec.js
